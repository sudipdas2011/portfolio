/* Removes any leftover structural border lines or grid guide columns */
.portfolio-content,
.hero-section,
.center-stage,
#hero-title {
  border: none !important;
  border-left: none !important;
  outline: none !important;
}

/* Forces layout to stay centered on screen */
.portfolio-content {
  width: 100vw;
  min-height: 100vh;
  margin: 0;
  padding: 0;
  overflow: hidden;
}
